#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll MOD = 1000000007; 

void sherlock(int t) {
    // int n, m, q;
    // cin >> n >> m >> q;
    // vector<int> book(n + 1, 1);
    // int p, r;
    // for (int i = 0; i < m; i++) {
    //     cin >> p;
    //     book[p] = 0;
    // }
    // ll ans = 0;
    // vector<int> dp(n + 1, -1);
    // for (int i = 0; i < q; i++) {
    //     cin >> r;
    //     if (dp[r] != -1) {
    //         ans += dp[r];
    //         continue;
    //     }
    //     int x = 0;
    //     for (int j = r; j <= n; j += r) {
    //         if (book[j] == 1) x++;
    //     }
    //     dp[r] = x;
    //     ans += x;
    // }
    // cout << "Case #" << t << ": " << ans << endl;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    // int tt;
    // cin >> tt;
    // for (int t = 1; t <= tt; t++) {
    //     sherlock(t);
    // }
    int a = 2;
    double b = 2.25454554443545;
    float c = 5.0;
    long d = 2;
    // float c = a + b;
    cout << (a + d) * (c + b) << endl;
}